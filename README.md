# Meeting Notes AI

This project uses FastAPI and OpenAI to extract, compress audio and convert speech to text from audio files. It also generates meeting minutes from the transcriptions.

## Usage

### Extract Audio

To extract audio from a file, make a POST request to `/extract_audio` with the input file path and output file path as parameters.

